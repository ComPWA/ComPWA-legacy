########### ComPWA Jamroot #############
import os ; 
path-constant TOP : . ;

#local ONMYMAC = [ os.environ ONMYMAC ] ;

#if $(ONMYMAC) { 
#using gcc : : /usr/bin/gcc ;
#using g++ : : /usr/bin/g++ ;
#}

project :
	requirements <include>$(TOP)
	<cxxflags>-ftemplate-depth-256
	<cxxflags>-std=c++0x
	;

# Configure the used packages here:
build-project Core ; 
build-project Physics ;
build-project DataReader ; 
build-project Estimator ;
build-project Optimizer ;
build-project Analysis ;
build-project test ;
